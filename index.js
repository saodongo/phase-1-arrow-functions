// divide function
const divide = function() {
    return 2000 / 100;
  };
  
  // square function
  const square = x => x * x;
  
  // Other functions (like foo, add, greet, etc.)
  const foo = function() {
    return 'bar';
  }
  
  const add = (parameter1, parameter2) => parameter1 + parameter2;
  
  // Function Declaration
  function greet(name) {
    return `Hello, ${name}!`;
  }
  
  const nums = [1, 2, 3];
  const squares = nums.map(x => x ** 2);  // Returns [1, 4, 9]
